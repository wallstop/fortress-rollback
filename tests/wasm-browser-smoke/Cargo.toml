[package]
name = "wasm-browser-smoke"
version = "0.0.0"
publish = false
edition = "2021"
rust-version = "1.86"

[dependencies]
fortress-rollback = { path = "../.." }

[target.'cfg(all(target_family = "wasm", target_os = "unknown"))'.dev-dependencies]
wasm-bindgen-test = "=0.3.56"

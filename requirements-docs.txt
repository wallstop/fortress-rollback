# Documentation build dependencies for MkDocs
# Using ~= (compatible release) constraints to allow patch updates
# while preventing breaking changes from major version updates.
mkdocs~=1.6.0
mkdocs-material~=9.5.0
pymdown-extensions~=10.14.0

2026-07-12 | Separate release preparation from publication and allow only one manual publish entrypoint | Tag-push plus workflow-dispatch publication | A reviewed version/changelog PR must pass normal CI before irreversible crates.io publication; removing the tag trigger prevents the manual workflow's tag push from racing a duplicate publisher, while checksum and tag-target validation make partial-failure retries fail closed | .github/workflows/release-prepare.yml; .github/workflows/publish.yml; scripts/tests/test_release_workflows.py | Issue #227
2026-07-16 | Build the network-test image from one filtered repository snapshot and one Cargo invocation | Hand-maintained Docker dependency-target placeholders | Keep Cargo target declarations and sources atomic; accept rebuilding dependencies when the source snapshot changes rather than allowing a duplicate target inventory to drift | .dockerignore; docker/Dockerfile; .github/workflows/ci-network.yml; scripts/tests/test_ci_rust_workflow.py | Issue #244
2026-07-17 | Discover every Cargo workspace root and synchronize its checked-in lock transactionally before release | Root-only textual lock rewriting and no-dependency validation | Prevent release PRs from passing with stale standalone locks while preserving Cargo as the lock authority | scripts/release/workspace_locks.py; scripts/release/prepare_release.py; .github/workflows/release-prepare.yml | PR #251 incident hardening

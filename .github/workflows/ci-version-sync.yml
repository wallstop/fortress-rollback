name: CI - Version Sync

on:
  push:
    branches: [main]
    paths:
      - "**.rs"
      - "**.md"
      - "**.toml"
      - "**.yml"
      - "**.yaml"
      - "**.sh"
      - "**.txt"
      - "**.json"
      - "**/Cargo.lock"
      - "scripts/release/**"
      - "scripts/sync-version.sh"
      - "scripts/hooks/check-wire-golden-immutable.py"
      - ".github/workflows/ci-version-sync.yml"
  pull_request:
    branches: [main]
    paths:
      - "**.rs"
      - "**.md"
      - "**.toml"
      - "**.yml"
      - "**.yaml"
      - "**.sh"
      - "**.txt"
      - "**.json"
      - "**/Cargo.lock"
      - "scripts/release/**"
      - "scripts/sync-version.sh"
      - "scripts/hooks/check-wire-golden-immutable.py"
      - ".github/workflows/ci-version-sync.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  version-sync:
    name: Version Sync Check
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Install stable Rust
        uses: dtolnay/rust-toolchain@02cb101ec7c40f2c49e1d9714d64511d8e1b74de # stable snapshot
        with:
          toolchain: stable

      - name: Check every Cargo workspace lock
        run: python3 scripts/release/workspace_locks.py check

      - name: Check Cargo/changelog/version references
        run: bash scripts/sync-version.sh --check

      - name: Check released wire golden immutability
        if: github.event_name == 'pull_request'
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
        run: python3 scripts/hooks/check-wire-golden-immutable.py --base-ref "$BASE_SHA"

SPECIFICATION Spec

\* Use small constants for bounded model checking
\* PEERS stays N=2: an N=3 bump exposes a SPEC-MODELING gap, not an impl bug.
\* syncHealth/pendingChecksums/lastVerifiedFrame are keyed [PEERS -> ...] (one
\* verdict per local peer), so a match against one remote clobbers a desync
\* verdict from another, violating the DesyncIsTerminal PROPERTY. The impl keys
\* per-(local,remote) pair correctly (src/sessions/p2p_session.rs). A faithful
\* N=3 version needs a [PEERS -> [PEERS -> ...]] restructuring (follow-up).
CONSTANT PEERS = {p1, p2}
CONSTANT MAX_FRAME = 4
CONSTANT CHECKSUM_INTERVAL = 2

CONSTRAINT StateConstraint

\* Safety invariants to check
INVARIANT TypeInvariant
INVARIANT NoFalsePositives
INVARIANT InSyncImpliesMatchingChecksums

\* Temporal properties (safety as temporal)
PROPERTY DesyncIsTerminal
PROPERTY VerifiedFrameMonotonic

\* Liveness properties (require fairness)
\* PROPERTY EventuallyInSync
\* PROPERTY EventuallyDetected
